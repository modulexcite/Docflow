@{
    ViewBag.Title = UIElementRes.UIElement.GroupProcesses;
}

<h2>@UIElementRes.UIElement.GroupProcesses</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetGroupProcessList", "GroupProcess")")

    var detailsUrl = '@Url.Action("Edit", "GroupProcess")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}