@{
    ViewBag.Title = UIElementRes.UIElement.Titles;
}

<h2>@UIElementRes.UIElement.Titles</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetTitleList", "Title")")

    var detailsUrl = '@Url.Action("Edit", "Title")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}