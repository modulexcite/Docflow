@{
    ViewBag.Title = UIElementRes.UIElement.Departments;
}

<h2>@UIElementRes.UIElement.Departments</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetDepartmentList", "Department")")

    var detailsUrl = '@Url.Action("Edit", "Department")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}