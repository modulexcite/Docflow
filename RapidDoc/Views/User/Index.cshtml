@{
    ViewBag.Title = UIElementRes.UIElement.Users;
}

<h2>@UIElementRes.UIElement.Users</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetUserList", "User")")

    var detailsUrl = '@Url.Action("Edit", "User")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}