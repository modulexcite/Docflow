@{
    ViewBag.Title = UIElementRes.UIElement.Profiles;
}

<h2>@UIElementRes.UIElement.Profiles</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetProfileList", "Profile")")

    var detailsUrl = '@Url.Action("Edit", "Profile")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}