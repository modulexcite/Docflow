@{
    ViewBag.Title = UIElementRes.UIElement.ServiceIncident;
}

<h2>@UIElementRes.UIElement.ServiceIncident</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
    grid_init("@Url.Action("GetServiceIncidentList", "ServiceIncident")")

    var detailsUrl = '@Url.Action("Edit", "ServiceIncident")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
}