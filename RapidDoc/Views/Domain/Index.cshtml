@{
    ViewBag.Title = UIElementRes.UIElement.Domains;
}

<h2>@UIElementRes.UIElement.Domains</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
<script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetDomainList", "Domain")")

    var detailsUrl = '@Url.Action("Edit", "Domain")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
</script>
}